var par = [];
var impar = [];

for (var i=1; i<=50; i++) {
    numeroAleatorio = (Math.floor((Math.random() * 100) + 1));
    if (numeroAleatorio % 2 == 0) {

        par.push(numeroAleatorio)
    
    }else {
        impar.push(numeroAleatorio)
    }
}

console.log(par)
console.log(impar)
